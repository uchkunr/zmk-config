# Enable Bluetooth signal boost (increases transmit power to max +8dBm)
# Helps with connection stability on macOS
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# Set custom keyboard name
CONFIG_ZMK_KEYBOARD_NAME="Corne"
